# Next.js app
OPENAI_API_KEY=
NEXT_PUBLIC_APP_URL=http://localhost:3000

# KIE (used by Next.js API routes when backend not configured)
KIE_API_KEY=

# Backend API (Lambda) - when set, upload & KIE use backend instead of Next.js routes
NEXT_PUBLIC_API_URL=
